# Example .env for TonToumaBot (do NOT commit secrets)
# Copy this to a local .env and fill values locally.

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/tontoumabot_db

# OpenAI (replace in your environment/CI, DO NOT commit)
OPENAI_API_KEY=REPLACE_ME

# MinIO / Storage
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=REPLACE_ME
MINIO_SECRET_KEY=REPLACE_ME

# Other secrets
SOME_OTHER_SECRET=REPLACE_ME
